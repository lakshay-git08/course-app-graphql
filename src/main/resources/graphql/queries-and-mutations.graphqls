type Query {
  getAllUsers(filter: UserFilter): [User]
  getUserById(id: ID!): User
}

type Mutation {
  createUser(input: UserInput!): User
  updateUser(id: ID!, input: UpdateUserInput!): User
  deleteUser(id: ID!): String
}

# '!' shows that the given field is required
